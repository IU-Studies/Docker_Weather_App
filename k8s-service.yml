apiVersion: v1
kind: Service
metadata:
  name: final-jury-service
spec:
  type: NodePort
  selector:
    app: final-jury
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30007